[build]
builder = "nixpacks"
buildCommand = "pnpm install --force && pnpm --filter api exec prisma generate"

[deploy]
startCommand = "pnpm --filter api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
